import urllib.request
import json
import ssl

def test():
    # Helper to authenticate and get token
    url = "http://127.0.0.1:8000/auth/login"
    data = {"email": "admin@crm.com", "senha": "123"}.get
    
    # We will just print the recent nohup logs for the GET request
test()
